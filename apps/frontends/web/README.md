# Web Frontend Placeholder

Planned responsibilities:

- operator dashboard
- findings inbox
- threaded chat
- approval controls for PR and deploy actions
- live runtime activity stream

The web frontend should talk to the `clog` backend over HTTP instead of embedding backend runtime code.
