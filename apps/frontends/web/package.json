{
  "name": "@clog/frontend-web",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "scripts": {
    "dev": "bun --bun vite build --watch",
    "build": "bun --bun vite build",
    "preview": "bun run build",
    "typecheck": "bun --bun svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@clog/types": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@types/bun": "^1.3.11",
    "svelte": "^5.55.1",
    "svelte-check": "^4.4.6",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  }
}
