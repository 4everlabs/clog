# Telegram Frontend

Telegram is a frontend workspace for `clog`.

It hosts the bot adapter and forwards operator messages into `@clog/core` without keeping Telegram-specific code inside the core runtime package.
