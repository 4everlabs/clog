# Telegram Frontend

Telegram is a frontend workspace for `clog`.

It hosts the bot adapter and forwards operator messages into the `clog` backend HTTP API without keeping Telegram-specific code inside the core backend package.
